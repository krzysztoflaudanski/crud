import styles from './Header.module.scss';
import NavBar from './../NavBar/NavBar'

const Header = () => {
    return (
        <section id='header'>
            <NavBar />
        </section>
    )
}

export default Header;